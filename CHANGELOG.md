# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed

- 数据库仿真引擎按 MySQL、PostgreSQL 协议拆分为独立模块，共享监听与审计编排，便于继续扩展数据库协议。
- 诱捕审计改为从运行实例操作菜单进入，按实例分页展示并在实例下线后自动销毁，单实例默认保留最新 10,000 条记录。
- 仿真引擎启动信息仅写入对应工作负载容器日志，不再作为诱捕审计事件展示。
- “规则整编”列表根据可用视口高度自动调整每页显示数量，减少大窗口下的空白区域。
- 增加 DNS TCP/UDP 双端点仿真，部署时只需配置一个主机端口，默认以 1053 同时发布 TCP 与 UDP。

### Fixed

- 批量下线规则实例时限制并发请求、逐项刷新完成状态，并按实例隔离抓包与下线生命周期操作，避免全局串行阻塞导致尾部实例下线失败。
- 批量下线结果通知明确展示成功与失败总数，并区分名单中省略的额外实例数量。
- 下线工作负载或清理 PCAP 失败时返回真实错误并保留失败实例，不再将失败状态误报为下线成功。
- 统一 PCAP 文件生成与安全清理的命名规则，修复已有抓包文件的实例在容器删除后无法完成下线的问题。
- MySQL 仿真会校验规则配置的 `mysql_native_password` 凭据，弱口令认证成功后支持版本查询及自定义查询响应。
- PostgreSQL 仿真会校验规则配置的凭据，并在认证成功后通过协议参数及版本查询返回配置的服务端版本。
- 抓包达到 5 分钟上限后由服务端自动完成并落盘；实例下线会同步清理活动抓包、持久化 PCAP 和实例数据，启动时会删除旧版本遗留的孤儿 PCAP。
- 部署规则实例时仅在确认按钮展示加载状态，避免全页面遮罩造成表格表头异常高亮。
- 下线规则实例时使用行级提交状态，避免全页面加载遮罩造成表格表头闪烁。

## [0.1.0-alpha.1] - 2026-08-09

### Added

- 首次发布协议仿真套件 API/UI 镜像和 engine workload 镜像。
- 支持导入协议仿真规则包并部署仿真实例。
- 支持 HTTP、Redis、SMTP、POP3、IMAP、SSH、FTP、RDP 协议仿真。
- 支持通过 Agent suite workload API 拉起和销毁规则容器。
- 支持实例审计、PCAP 取证、空包通知和浏览器预览能力。
- 前端接入 SecLab UI、SDL Token 和套件 SDK 的主题、语言、导航能力。
- 接入 SecLab 套件语义操作日志，记录规则、仿真实例、抓包生命周期和 PCAP 下载结果。

### Changed

- Agent 接入改为读取 SecLab 注入的统一运行时描述，在本地 UDS 和节点 HTTPS 环境使用相同业务客户端。
- 工作负载和抓包请求改用套件实例令牌授权，PCAP 完成接口直接接收二进制数据。
- HTTP 仿真规则的首屏 HTML 改为在套件内安全预览，不再依赖主控内置浏览器应用。

### Fixed

- 修复批量扫描协议实例时审计事件并发写入导致 SQLite 锁竞争、连接池耗尽和应用卡顿的问题。
- 修复 HTTP 500 日志缺少请求上下文和底层错误链、难以定位故障的问题。
- 修复套件操作通知未通过主控展示、导致部署成功后没有反馈的问题。
- 修复部署仿真规则后自动跳转到实例部署页、影响连续部署的问题。
- 修复部署确认按钮可重复提交，导致同一端口创建重复实例的问题。
- 部署前检查实例端口与目标节点宿主机端口，冲突时显示本地化通知且不保留失败实例。
